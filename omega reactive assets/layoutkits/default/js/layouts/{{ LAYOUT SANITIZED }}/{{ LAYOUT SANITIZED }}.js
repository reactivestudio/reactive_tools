(function ($, Drupal) {
  /**
   * Toggle show/hide links for off canvas layout.
   */
  Drupal.behaviors.omega{{ LAYOUT CAMELCASE LOWER }}Layout = {
    attach: function (context) {
        // code here
    }
  };

})(jQuery, Drupal);
