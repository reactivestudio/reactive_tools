(function ($, Drupal) {
  /**
   * Toggle show/hide links for off canvas layout.
   */
  Drupal.behaviors.omega{{ LAYOUT CAMELCASE }}Layout = {
    attach: function (context) {

    }
  };

})(jQuery, Drupal);
